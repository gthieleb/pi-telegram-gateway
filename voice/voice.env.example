# pi-telegram-gateway voice sidecar — copy to ~/.pi/agent/pi-telegram-gateway/voice.env
# NEVER commit the real file (gitignored as voice.env).
API_ID=0
API_HASH=
BOT_TOKEN=123456:REPLACE_ME
# idle self-exit of the python worker (seconds)
IDLE_EXIT_SECONDS=600